var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { useForwardedRef, useCustomValidity } from '../../hooks';
import { radioBaseStyles, inputElementStyles } from './styles';
const Root = styled.div(radioBaseStyles);
const Input = styled.input(inputElementStyles);
export const Radio = forwardRef((props, forwardedRef) => {
    const { className, style, customValidity } = props, restProps = __rest(props, ["className", "style", "customValidity"]);
    const ref = useForwardedRef(forwardedRef);
    useCustomValidity(ref, customValidity);
    return (React.createElement(Root, { className: className, "data-ui": "Radio", style: style },
        React.createElement(Input, Object.assign({ type: "radio" }, restProps, { ref: ref })),
        React.createElement("span", null)));
});
Radio.displayName = 'Radio';
//# sourceMappingURL=radio.js.map
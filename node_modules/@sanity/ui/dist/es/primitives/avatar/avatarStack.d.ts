import React from 'react';
import { AvatarSize } from './types';
interface AvatarStackProps {
    children: React.ReactNode;
    maxLength?: number;
    size?: AvatarSize | AvatarSize[];
    tone?: 'navbar';
}
export declare function AvatarStack(props: AvatarStackProps & Omit<React.HTMLProps<HTMLDivElement>, 'as' | 'ref'>): JSX.Element;
export {};

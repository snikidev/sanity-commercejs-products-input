var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { responsivePaddingStyle, responsiveRadiusStyle, } from '../../styles';
import { Label } from '../label';
import { badge } from './styles';
const Root = styled.div(responsivePaddingStyle, responsiveRadiusStyle, badge);
export const Badge = forwardRef((props, ref) => {
    const { children, mode = 'default', padding = 1, radius = 2, size, tone = 'default' } = props, restProps = __rest(props, ["children", "mode", "padding", "radius", "size", "tone"]);
    return (React.createElement(Root, Object.assign({ "data-ui": "Badge" }, restProps, { mode: mode, padding: padding, ref: ref, tone: tone, radius: radius }),
        React.createElement(Label, { size: size }, children)));
});
Badge.displayName = 'Badge';
//# sourceMappingURL=badge.js.map
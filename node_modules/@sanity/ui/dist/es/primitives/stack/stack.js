var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import styled from 'styled-components';
import { getResponsiveProp } from '../../styles';
import { stackBaseStyles, stackSpaceStyles } from './styles';
const Root = styled.div(stackBaseStyles, stackSpaceStyles);
export const Stack = forwardRef((props, ref) => {
    const { space: spaceProp } = props, restProps = __rest(props, ["space"]);
    const space = getResponsiveProp(spaceProp, []);
    return React.createElement(Root, Object.assign({ "data-ui": "Stack" }, restProps, { ref: ref, space: space }));
});
Stack.displayName = 'Stack';
//# sourceMappingURL=stack.js.map
"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.color = void 0;
var color_1 = require("@sanity/color");
var color_fns_1 = require("../lib/color-fns");
var theme_1 = require("../lib/theme");
var helpers_1 = require("./helpers");
var tones = {
    default: color_1.hues.gray,
    primary: color_1.hues.blue,
    positive: color_1.hues.green,
    caution: color_1.hues.yellow,
    critical: color_1.hues.red,
};
exports.color = theme_1.createColorTheme({
    base: function (_a) {
        var dark = _a.dark, name = _a.name;
        if (name === 'default') {
            return {
                fg: dark ? color_1.white.hex : color_1.black.hex,
                bg: dark ? color_1.black.hex : color_1.white.hex,
                border: color_1.hues.gray[dark ? 800 : 200].hex,
                focusRing: color_1.hues.blue[500].hex,
                shadow: {
                    outline: color_fns_1.rgba(color_1.hues.gray[500].hex, 0.35),
                    umbra: color_fns_1.rgba(dark ? color_1.black.hex : color_1.hues.gray[500].hex, 0.2),
                    penumbra: color_fns_1.rgba(dark ? color_1.black.hex : color_1.hues.gray[500].hex, 0.14),
                    ambient: color_fns_1.rgba(dark ? color_1.black.hex : color_1.hues.gray[500].hex, 0.12),
                },
            };
        }
        if (name === 'transparent') {
            var tints_1 = tones.default;
            return {
                fg: tints_1[dark ? 100 : 900].hex,
                bg: tints_1[dark ? 950 : 50].hex,
                border: tints_1[dark ? 800 : 200].hex,
                focusRing: color_1.hues.blue[500].hex,
                shadow: {
                    outline: color_fns_1.rgba(tints_1[500].hex, 0.35),
                    umbra: color_fns_1.rgba(dark ? color_1.black.hex : tints_1[500].hex, 0.2),
                    penumbra: color_fns_1.rgba(dark ? color_1.black.hex : tints_1[500].hex, 0.14),
                    ambient: color_fns_1.rgba(dark ? color_1.black.hex : tints_1[500].hex, 0.12),
                },
            };
        }
        var tints = tones[name] || tones.default;
        return {
            fg: tints[dark ? 100 : 900].hex,
            bg: tints[dark ? 950 : 50].hex,
            border: tints[dark ? 800 : 200].hex,
            focusRing: tints[500].hex,
            shadow: {
                outline: color_fns_1.rgba(tints[500].hex, 0.35),
                umbra: color_fns_1.rgba(dark ? color_1.black.hex : tints[500].hex, 0.2),
                penumbra: color_fns_1.rgba(dark ? color_1.black.hex : tints[500].hex, 0.14),
                ambient: color_fns_1.rgba(dark ? color_1.black.hex : tints[500].hex, 0.12),
            },
        };
    },
    solid: function (_a) {
        var base = _a.base, dark = _a.dark, state = _a.state, tone = _a.tone;
        var mix = dark ? helpers_1.screen : helpers_1.multiply;
        var tints = tones[tone] || tones.default;
        if (state === 'disabled') {
            return {
                bg: mix(base.bg, color_1.hues.gray[dark ? 800 : 200].hex),
                border: mix(base.bg, color_1.hues.gray[dark ? 900 : 100].hex),
                fg: mix(base.bg, dark ? color_1.black.hex : color_1.white.hex),
            };
        }
        if (state === 'hovered') {
            return {
                bg: mix(base.bg, tints[dark ? 300 : 600].hex),
                border: mix(base.bg, tints[dark ? 200 : 700].hex),
                fg: mix(base.bg, dark ? color_1.black.hex : color_1.white.hex),
            };
        }
        if (state === 'pressed') {
            return {
                bg: mix(base.bg, tints[dark ? 300 : 700].hex),
                border: mix(base.bg, tints[dark ? 200 : 800].hex),
                fg: mix(base.bg, dark ? color_1.black.hex : color_1.white.hex),
            };
        }
        if (state === 'selected') {
            return {
                bg: mix(base.bg, tints[dark ? 200 : 800].hex),
                border: mix(base.bg, tints[dark ? 100 : 900].hex),
                fg: mix(base.bg, dark ? color_1.black.hex : color_1.white.hex),
            };
        }
        // state: "enabled" | unknown
        return {
            bg: mix(base.bg, tints[dark ? 400 : 500].hex),
            border: mix(base.bg, tints[dark ? 300 : 600].hex),
            fg: mix(base.bg, dark ? color_1.black.hex : color_1.white.hex),
        };
    },
    muted: function (_a) {
        var base = _a.base, dark = _a.dark, state = _a.state, tone = _a.tone;
        var mix = dark ? helpers_1.screen : helpers_1.multiply;
        var tints = tones[tone] || tones.default;
        if (state === 'disabled') {
            return {
                bg: mix(base.bg, color_1.hues.gray[dark ? 950 : 50].hex),
                border: mix(base.bg, color_1.hues.gray[dark ? 900 : 100].hex),
                fg: mix(base.bg, color_1.hues.gray[dark ? 800 : 200].hex),
            };
        }
        if (state === 'hovered') {
            return {
                bg: mix(base.bg, tints[dark ? 900 : 100].hex),
                border: mix(base.bg, tints[dark ? 200 : 700].hex),
                fg: mix(base.bg, tints[dark ? 300 : 700].hex),
            };
        }
        if (state === 'pressed') {
            return {
                bg: mix(base.bg, tints[dark ? 900 : 100].hex),
                border: mix(base.bg, tints[dark ? 800 : 200].hex),
                fg: mix(base.bg, tints[dark ? 200 : 800].hex),
            };
        }
        if (state === 'selected') {
            return {
                bg: mix(base.bg, tints[dark ? 950 : 50].hex),
                border: mix(base.bg, tints[dark ? 100 : 900].hex),
                fg: mix(base.bg, tints[dark ? 300 : 700].hex),
            };
        }
        return {
            bg: mix(base.bg, tints[dark ? 950 : 50].hex),
            border: mix(base.bg, tints[dark ? 900 : 100].hex),
            fg: mix(base.bg, tints[dark ? 400 : 600].hex),
        };
    },
    button: function (_a) {
        var base = _a.base, mode = _a.mode, muted = _a.muted, solid = _a.solid;
        if (mode === 'bleed') {
            return {
                enabled: __assign(__assign({}, muted.enabled), { bg: base.bg, border: base.bg }),
                hovered: __assign(__assign({}, muted.hovered), { bg: muted.enabled.bg, border: muted.enabled.bg }),
                pressed: __assign(__assign({}, muted.pressed), { border: muted.pressed.bg }),
                disabled: __assign(__assign({}, muted.disabled), { bg: base.bg, border: base.bg }),
                selected: __assign(__assign({}, muted.selected), { border: muted.selected.bg }),
            };
        }
        if (mode === 'ghost') {
            return {
                enabled: __assign(__assign({}, muted.enabled), { bg: base.bg, border: base.border }),
                hovered: __assign(__assign({}, solid.hovered), { border: solid.hovered.bg }),
                pressed: __assign(__assign({}, solid.pressed), { border: solid.pressed.bg }),
                selected: __assign(__assign({}, solid.selected), { border: solid.selected.bg }),
                disabled: __assign(__assign({}, muted.disabled), { bg: base.bg, border: muted.disabled.border }),
            };
        }
        return {
            enabled: __assign(__assign({}, solid.enabled), { border: solid.enabled.bg }),
            hovered: __assign(__assign({}, solid.hovered), { border: solid.hovered.bg }),
            pressed: __assign(__assign({}, solid.pressed), { border: solid.pressed.bg }),
            selected: __assign(__assign({}, solid.selected), { border: solid.selected.bg }),
            disabled: __assign(__assign({}, solid.disabled), { border: solid.disabled.bg }),
        };
    },
    input: function (_a) {
        var base = _a.base, dark = _a.dark, mode = _a.mode, state = _a.state;
        var mix = dark ? helpers_1.screen : helpers_1.multiply;
        if (mode === 'invalid') {
            var tints = tones.critical;
            return {
                bg: mix(base.bg, tints[dark ? 950 : 50].hex),
                fg: mix(base.bg, tints[dark ? 300 : 700].hex),
                border: mix(base.bg, tints[dark ? 800 : 200].hex),
                placeholder: mix(base.bg, tints[dark ? 300 : 700].hex),
            };
        }
        if (state === 'hovered') {
            return {
                bg: base.bg,
                fg: base.fg,
                border: mix(base.bg, color_1.hues.gray[dark ? 700 : 300].hex),
                placeholder: mix(base.bg, color_1.hues.gray[dark ? 300 : 700].hex),
            };
        }
        if (state === 'disabled') {
            return {
                bg: mix(base.bg, color_1.hues.gray[dark ? 950 : 50].hex),
                fg: mix(base.bg, color_1.hues.gray[dark ? 800 : 200].hex),
                border: mix(base.bg, color_1.hues.gray[dark ? 900 : 100].hex),
                placeholder: mix(base.bg, color_1.hues.gray[dark ? 900 : 100].hex),
            };
        }
        return {
            bg: base.bg,
            fg: base.fg,
            border: base.border,
            placeholder: mix(base.bg, color_1.hues.gray[dark ? 300 : 700].hex),
        };
    },
    card: function (_a) {
        var base = _a.base, dark = _a.dark, muted = _a.muted, name = _a.name, state = _a.state;
        var mix = dark ? helpers_1.screen : helpers_1.multiply;
        if (state === 'selected') {
            mix = dark ? helpers_1.multiply : helpers_1.screen;
            var tint = ['default', 'transparent'].includes(name) ? color_1.hues.blue : tones[name];
            var bg = tint[dark ? 400 : 500].hex;
            return {
                bg: bg,
                fg: dark ? color_1.black.hex : color_1.white.hex,
                border: tint[dark ? 600 : 400].hex,
                muted: {
                    fg: mix(bg, color_1.hues.gray[dark ? 600 : 500].hex),
                },
                accent: {
                    fg: mix(bg, color_1.hues.red[dark ? 600 : 500].hex),
                },
                link: {
                    fg: mix(bg, color_1.hues.blue[dark ? 600 : 500].hex),
                },
                code: {
                    bg: mix(bg, color_1.hues.gray[dark ? 50 : 950].hex),
                    fg: mix(bg, color_1.hues.gray[dark ? 600 : 500].hex),
                },
            };
        }
        if (state === 'hovered') {
            return __assign(__assign({}, muted.hovered), { fg: base.fg, muted: {
                    fg: mix(muted.hovered.bg, color_1.hues.gray[dark ? 400 : 600].hex),
                }, accent: {
                    fg: mix(muted.hovered.bg, color_1.hues.red[dark ? 500 : 500].hex),
                }, link: {
                    fg: mix(muted.hovered.bg, color_1.hues.blue[dark ? 400 : 600].hex),
                }, code: {
                    bg: mix(muted.hovered.bg, color_1.hues.gray[dark ? 950 : 50].hex),
                    fg: color_1.hues.gray[dark ? 400 : 600].hex,
                } });
        }
        if (state === 'pressed') {
            return __assign(__assign({}, muted.pressed), { fg: base.fg, muted: {
                    fg: mix(muted.pressed.bg, color_1.hues.gray[dark ? 400 : 600].hex),
                }, accent: {
                    fg: mix(muted.pressed.bg, color_1.hues.red[dark ? 500 : 500].hex),
                }, link: {
                    fg: mix(muted.pressed.bg, color_1.hues.blue[dark ? 400 : 600].hex),
                }, code: {
                    bg: mix(muted.pressed.bg, color_1.hues.gray[dark ? 950 : 50].hex),
                    fg: color_1.hues.gray[dark ? 400 : 600].hex,
                } });
        }
        if (state === 'disabled') {
            return __assign(__assign({}, muted.disabled), { muted: {
                    fg: muted.disabled.fg,
                }, accent: {
                    fg: muted.disabled.fg,
                }, link: {
                    fg: muted.disabled.fg,
                }, code: {
                    bg: 'transparent',
                    fg: muted.disabled.fg,
                } });
        }
        return {
            bg: base.bg,
            fg: base.fg,
            border: base.border,
            muted: {
                fg: mix(base.bg, color_1.hues.gray[dark ? 400 : 600].hex),
            },
            accent: {
                fg: mix(base.bg, color_1.hues.red[dark ? 500 : 500].hex),
            },
            link: {
                fg: mix(base.bg, color_1.hues.blue[dark ? 400 : 600].hex),
            },
            code: {
                bg: mix(base.bg, color_1.hues.gray[dark ? 950 : 50].hex),
                fg: color_1.hues.gray[dark ? 400 : 600].hex,
            },
        };
    },
    spot: function (_a) {
        var base = _a.base, dark = _a.dark, key = _a.key;
        var mix = dark ? helpers_1.screen : helpers_1.multiply;
        return mix(base.bg, color_1.hues[key][dark ? 400 : 500].hex);
    },
    syntax: function (_a) {
        var base = _a.base, dark = _a.dark;
        var mix = dark ? helpers_1.screen : helpers_1.multiply;
        var mainShade = dark ? 400 : 600;
        var secondaryShade = dark ? 600 : 400;
        return {
            atrule: mix(base.bg, color_1.hues.purple[mainShade].hex),
            attrName: mix(base.bg, color_1.hues.green[mainShade].hex),
            attrValue: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            attribute: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            boolean: mix(base.bg, color_1.hues.purple[mainShade].hex),
            builtin: mix(base.bg, color_1.hues.purple[mainShade].hex),
            cdata: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            char: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            class: mix(base.bg, color_1.hues.orange[mainShade].hex),
            className: mix(base.bg, color_1.hues.cyan[mainShade].hex),
            comment: mix(base.bg, color_1.hues.gray[secondaryShade].hex),
            constant: mix(base.bg, color_1.hues.purple[mainShade].hex),
            deleted: mix(base.bg, color_1.hues.red[mainShade].hex),
            doctype: mix(base.bg, color_1.hues.gray[secondaryShade].hex),
            entity: mix(base.bg, color_1.hues.red[mainShade].hex),
            function: mix(base.bg, color_1.hues.green[mainShade].hex),
            hexcode: mix(base.bg, color_1.hues.blue[mainShade].hex),
            id: mix(base.bg, color_1.hues.purple[mainShade].hex),
            important: mix(base.bg, color_1.hues.purple[mainShade].hex),
            inserted: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            keyword: mix(base.bg, color_1.hues.magenta[mainShade].hex),
            number: mix(base.bg, color_1.hues.purple[mainShade].hex),
            operator: mix(base.bg, color_1.hues.magenta[mainShade].hex),
            prolog: mix(base.bg, color_1.hues.gray[secondaryShade].hex),
            property: mix(base.bg, color_1.hues.blue[mainShade].hex),
            pseudoClass: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            pseudoElement: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            punctuation: mix(base.bg, color_1.hues.gray[mainShade].hex),
            regex: mix(base.bg, color_1.hues.blue[mainShade].hex),
            selector: mix(base.bg, color_1.hues.red[mainShade].hex),
            string: mix(base.bg, color_1.hues.yellow[mainShade].hex),
            symbol: mix(base.bg, color_1.hues.purple[mainShade].hex),
            tag: mix(base.bg, color_1.hues.red[mainShade].hex),
            unit: mix(base.bg, color_1.hues.orange[mainShade].hex),
            url: mix(base.bg, color_1.hues.red[mainShade].hex),
            variable: mix(base.bg, color_1.hues.red[mainShade].hex),
        };
    },
});
//# sourceMappingURL=color.js.map
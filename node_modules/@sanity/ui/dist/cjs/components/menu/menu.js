"use strict";
var __makeTemplateObject = (this && this.__makeTemplateObject) || function (cooked, raw) {
    if (Object.defineProperty) { Object.defineProperty(cooked, "raw", { value: raw }); } else { cooked.raw = raw; }
    return cooked;
};
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Menu = void 0;
var react_1 = __importStar(require("react"));
var styled_components_1 = __importDefault(require("styled-components"));
var helpers_1 = require("../../helpers");
var hooks_1 = require("../../hooks");
var primitives_1 = require("../../primitives");
var utils_1 = require("../../utils");
var menuContext_1 = require("./menuContext");
var Root = styled_components_1.default(primitives_1.Box)(templateObject_1 || (templateObject_1 = __makeTemplateObject(["\n  outline: none;\n"], ["\n  outline: none;\n"])));
exports.Menu = react_1.forwardRef(function (props, ref) {
    var children = props.children, focusLast = props.focusLast, onClickOutside = props.onClickOutside, onEscape = props.onEscape, onItemClick = props.onItemClick, _a = props.padding, padding = _a === void 0 ? 1 : _a, _b = props.space, space = _b === void 0 ? 1 : _b, restProps = __rest(props, ["children", "focusLast", "onClickOutside", "onEscape", "onItemClick", "padding", "space"]);
    var isTopLayer = utils_1.useLayer().isTopLayer;
    var _c = react_1.useState(null), rootElement = _c[0], setRootElement = _c[1];
    var itemsRef = react_1.useRef([]);
    var _d = react_1.useState(-1), activeIndex = _d[0], setActiveIndex = _d[1];
    react_1.useEffect(function () {
        if (rootElement) {
            if (focusLast) {
                if (helpers_1.focusLastDescendant(rootElement)) {
                    setActiveIndex(itemsRef.current.indexOf(document.activeElement));
                }
            }
            else {
                if (helpers_1.focusFirstDescendant(rootElement)) {
                    setActiveIndex(itemsRef.current.indexOf(document.activeElement));
                }
            }
        }
    }, [focusLast, rootElement]);
    var setRef = function (el) {
        setRootElement(el);
        if (typeof ref === 'function')
            ref(el);
        else if (ref)
            ref.current = el;
    };
    var mount = react_1.useCallback(function (element) {
        if (!element)
            return function () { return undefined; };
        if (!itemsRef.current.includes(element)) {
            itemsRef.current.push(element);
        }
        return function () {
            var idx = itemsRef.current.push(element);
            if (idx > -1) {
                itemsRef.current.splice(idx, 1);
            }
        };
    }, []);
    var handleKeyDown = react_1.useCallback(function (event) {
        if (event.key === 'ArrowUp') {
            event.preventDefault();
            // NOTE: if all menu items are :disabled, this will cause an infinite loop
            // @todo: make sure that not all menu items are disabled
            var len = itemsRef.current.length;
            var currentIndex = activeIndex;
            var element = null;
            while (!element) {
                currentIndex = (currentIndex - 1 + len) % len;
                var e = itemsRef.current[currentIndex];
                if (helpers_1.isHTMLButtonElement(e) && !e.disabled) {
                    element = itemsRef.current[currentIndex];
                }
            }
            setActiveIndex(currentIndex);
            if (element)
                element.focus();
            return;
        }
        if (event.key === 'ArrowDown') {
            event.preventDefault();
            // NOTE: if all menu items are :disabled, this will cause an infinite loop
            // @todo: make sure that not all menu items are disabled
            var len = itemsRef.current.length;
            var currentIndex = activeIndex;
            var element = null;
            while (!element) {
                currentIndex = (currentIndex + 1) % len;
                var e = itemsRef.current[currentIndex];
                if (helpers_1.isHTMLButtonElement(e) && !e.disabled) {
                    element = itemsRef.current[currentIndex];
                }
            }
            setActiveIndex(currentIndex);
            if (element)
                element.focus();
            return;
        }
    }, [activeIndex]);
    var handleItemMouseEnter = react_1.useCallback(function (event) {
        var element = event.currentTarget;
        setActiveIndex(itemsRef.current.indexOf(element));
        element.focus();
    }, []);
    var handleItemMouseLeave = react_1.useCallback(function () {
        setActiveIndex(-1);
        rootElement === null || rootElement === void 0 ? void 0 : rootElement.focus();
    }, [rootElement]);
    hooks_1.useClickOutside(react_1.useCallback(function () { return isTopLayer && onClickOutside && onClickOutside(); }, [
        isTopLayer,
        onClickOutside,
    ]), [rootElement]);
    hooks_1.useGlobalKeyDown(react_1.useCallback(function (event) {
        if (!isTopLayer)
            return;
        if (event.key === 'Escape') {
            event.stopPropagation();
            if (onEscape)
                onEscape();
        }
    }, [isTopLayer, onEscape]));
    return (react_1.default.createElement(menuContext_1.MenuContext.Provider, { value: {
            activeIndex: activeIndex,
            mount: mount,
            onMouseEnter: handleItemMouseEnter,
            onMouseLeave: handleItemMouseLeave,
            onItemClick: onItemClick,
        } },
        react_1.default.createElement(Root, __assign({ "data-ui": "Menu" }, restProps, { onKeyDown: handleKeyDown, padding: padding, ref: setRef, role: "menu", tabIndex: -1 }),
            react_1.default.createElement(primitives_1.Stack, { space: space }, children))));
});
exports.Menu.displayName = 'Menu';
var templateObject_1;
//# sourceMappingURL=menu.js.map